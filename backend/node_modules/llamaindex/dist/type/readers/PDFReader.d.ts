import { Document, FileReader } from "@llamaindex/core/schema";
/**
 * Read the text of a PDF
 */
export declare class PDFReader extends FileReader {
    loadDataAsContent(content: Uint8Array): Promise<Document[]>;
}
