import { Document, FileReader } from "@llamaindex/core/schema";
export declare class DocxReader extends FileReader {
    /** DocxParser */
    loadDataAsContent(fileContent: Uint8Array): Promise<Document[]>;
}
